# DISPOSABLE demo stack

Proves the Lambda@Edge adapter end-to-end against Enhancely STAGING
(demo.staging.enhancely.ai -> CloudFront -> Lambda@Edge -> S3 website).
See main.tf header. Local state on purpose. `tofu destroy` after review.
