{
  "name": "@enhancely/adapter-cloudflare",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker reference adapter: injects Enhancely JSON-LD into origin HTML via HTMLRewriter (fail-open). Bundling is done by wrangler at deploy time; 'build' is a typecheck only.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@enhancely/injector-core": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.0",
    "wrangler": "^3.100.0"
  }
}
